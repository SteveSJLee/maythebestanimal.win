[Unit]
Description=MariaDB Docker Container
After=docker.service
Requires=docker.service

[Service]
Type=simple
Restart=on-failure
RestartSec=10
ExecStart=/usr/bin/docker start -a mariadb
ExecStop=/usr/bin/docker stop -t 5 mariadb

[Install]
WantedBy=multi-user.target
